import { Component } from '@angular/core';

@Component({
    selector: 'sandbox', // name of html tag this component will control
    // using back ticks instread of single quotes allows entering of template strings spanning multiple lines
    template: `<h1>Hello World</h1>`
})

export class SandboxComponent {

}